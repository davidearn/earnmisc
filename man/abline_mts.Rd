% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{abline_mts}
\alias{abline_mts}
\title{Add reference lines to mts plot panels}
\usage{
abline_mts(
  plot.info = NULL,
  panels = NULL,
  columns = NULL,
  a = NULL,
  b = NULL,
  h = NULL,
  v = NULL,
  reg = NULL,
  coef = NULL,
  untf = FALSE,
  source = "abline",
  record = TRUE,
  col = NULL,
  lty = NULL,
  lwd = NULL,
  ...
)
}
\arguments{
\item{plot.info}{Plot metadata returned by \code{\link[=plot_mts]{plot_mts()}}. If \code{NULL}, the most
recent \code{plot_mts()} result is used.}

\item{panels}{Optional full layout panel indices. If supplied, these panels
are used directly and may include blank panels.}

\item{columns}{Optional plotted data columns. Character values are matched to
plotted column names; numeric values are interpreted as positions among the
plotted columns.}

\item{a, b, h, v, reg, coef, untf}{Arguments passed to \code{\link[graphics:abline]{graphics::abline()}}. Only
non-\code{NULL} line-definition arguments are passed.}

\item{source}{Source label used when recording the reference lines. It may be
a non-empty character scalar or scalar expression-like label, including
output from \code{\link[=nice_text]{nice_text()}}.}

\item{record}{Logical. If \code{TRUE}, append reference-line rows to the
\code{plot.info$curves} registry for later legend construction.}

\item{col, lty, lwd}{Common graphical arguments passed to
\code{\link[graphics:abline]{graphics::abline()}}. They may be scalar, vectorised by selected panel, or
named by plotted column where possible.}

\item{...}{Additional graphical arguments passed to \code{\link[graphics:abline]{graphics::abline()}}.}
}
\value{
Invisibly returns the updated \code{earnmisc_mts_plot_info} object.
}
\description{
Add \code{\link[graphics:abline]{graphics::abline()}} reference lines to panels created by \code{\link[=plot_mts]{plot_mts()}}.
This helper is part of the \code{plot_mts()} workflow and is not intended for
arbitrary existing \code{plot.mts()} output. By default, it draws on all data
panels and skips reserved blank panels.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
plot.info <- plot_mts(x)
plot.info <- abline_mts(
  plot.info = plot.info,
  h = 0,
  col = "grey70",
  lty = 2
)

plot.info <- plot_mts(x, blank.panels = 1)
plot.info <- abline_mts(plot.info = plot.info, h = 0)
legend_mts(plot.info)

}
