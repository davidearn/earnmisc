% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nice_text.R
\name{nice_text_macros}
\alias{nice_text_macros}
\title{Active TeX macros for \code{nice_text()}}
\usage{
nice_text_macros(macros.file = NULL, append.macros = TRUE)
}
\arguments{
\item{macros.file}{Optional path to a user TeX macro file. If \code{NULL}, only
the option \code{earnmisc.tex_macros_file} is checked.}

\item{append.macros}{Logical scalar. If \code{TRUE}, include package defaults. If
\code{FALSE}, use only user files.}
}
\value{
A named character vector. Names are macro names without the leading
backslash and values are replacement strings.
}
\description{
Return the active no-argument TeX macro definitions used by \code{nice_text()}.
}
\details{
Package defaults are read from the curated earnmisc file
\code{inst/tex/default-macros.tex}. This file is intended for stable
cross-package plot-label notation, not as a full manuscript preamble. If the
option \code{earnmisc.tex_macros_file} is set, that file is read after the
defaults. If \code{macros.file} is supplied, it is read after the option file.
Later definitions override earlier definitions with the same macro name. Set
\code{append.macros = FALSE} to omit the package defaults.
}
\examples{
nice_text_macros()

macros.file <- tempfile(fileext = ".tex")
writeLines("\\\\newcommand{\\\\foo}{bar}", macros.file)
nice_text_macros(macros.file = macros.file)

}
