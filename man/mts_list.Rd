% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_list.R
\name{mts_list}
\alias{mts_list}
\title{Native-grid multivariate time-series lists}
\usage{
mts_list(x, time = NULL, names = NULL, ...)
}
\arguments{
\item{x}{Non-empty list of univariate numeric series. Each element may be a
numeric vector, a univariate \code{ts} object, or a list with components \code{time}
and \code{value}.}

\item{time}{Optional list of numeric time vectors, one for each element of
\code{x}. If \code{NULL}, times are taken from \code{ts} elements, from per-element
\code{time} components, or from \code{seq_along(value)}.}

\item{names}{Optional character vector of panel/series names. If \code{NULL},
names are taken from \code{x} and missing names are replaced by element indices.}

\item{...}{Additional arguments are currently unsupported and must be empty.}
}
\value{
A list with class \code{c("mts_list", "list")}. Each element is a list
with numeric components \code{time} and \code{value}.
}
\description{
\code{mts_list()} creates a list-like multivariate time-series object for panel
plotting when each series has its own native time grid. Each element stores
one numeric time vector and one numeric value vector. Use \code{\link[=mts_plot]{mts_plot()}} to draw
one panel per element.
}
\details{
\code{mts_list} objects are intended for native-grid panel plotting. They do not
interpolate series onto a common grid, and \code{\link[=mts_lines]{mts_lines()}} overlay support is
not implemented for them.
}
\examples{
x <- mts_list(
  list(a = 1:5, b = 11:16),
  time = list(a = 0:4, b = seq(0, 10, length.out = 6))
)
mts_plot(x, nrow = 1, ncol = 2)

}
