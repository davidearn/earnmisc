% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aligned_text.R
\name{aligned_text}
\alias{aligned_text}
\title{Draw an aligned three-column text block}
\usage{
aligned_text(
  x,
  y = NULL,
  lhs,
  mid,
  rhs,
  inset = 0.02,
  cex = 1,
  col = graphics::par("col"),
  line.spacing = 1.25,
  gap = NULL,
  lhs.adj = 1,
  mid.adj = 0.5,
  rhs.adj = 0,
  use.tikz = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric x-coordinate for the top-left anchor, or one of the
legend-style keyword positions \code{"bottomright"}, \code{"bottom"},
\code{"bottomleft"}, \code{"left"}, \code{"center"}, \code{"right"}, \code{"topleft"}, \code{"top"},
or \code{"topright"}. The spelling \code{"centre"} is also accepted.}

\item{y}{Numeric y-coordinate for the top-left anchor when \code{x} is numeric.
Must be \code{NULL} when \code{x} is a keyword.}

\item{lhs, mid, rhs}{Character vectors for the left, middle, and right
columns. They must have the same positive length.}

\item{inset}{Numeric scalar or length-two vector. For keyword placement,
gives the inset from the plot region as a fraction of the plotting range,
following the same convention as \code{\link[graphics:legend]{graphics::legend()}}. A scalar is used
for both horizontal and vertical insets.}

\item{cex}{Text size passed to \code{\link[graphics:text]{graphics::text()}}.}

\item{col}{Text colour passed to \code{\link[graphics:text]{graphics::text()}}.}

\item{line.spacing}{Positive multiplier applied to the maximum row height
to determine vertical spacing between rows.}

\item{gap}{Horizontal gap between adjacent columns in user coordinates. If
\code{NULL}, the width of \code{"m"} at the requested \code{cex} is used.}

\item{lhs.adj, mid.adj, rhs.adj}{Horizontal adjustment values passed to
\code{\link[graphics:text]{graphics::text()}} for the corresponding column. Defaults are
\code{lhs.adj = 1}, \code{mid.adj = 0.5}, and \code{rhs.adj = 0}, giving
right-justified, centred, and left-justified columns.}

\item{use.tikz}{Optional logical scalar passed to \code{\link[=nice_text]{nice_text()}}. If \code{NULL},
\code{\link[=nice_text]{nice_text()}} resolves tikz mode from the caller or active graphics device.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:text]{graphics::text()}}, for
example \code{font}, \code{family}, or \code{xpd}. The \code{x}, \code{y}, \code{labels}, and \code{adj}
arguments are controlled by \code{aligned_text()} and cannot be supplied in
\code{...}.}
}
\value{
Invisibly returns an \code{earnmisc_aligned_text_info} list containing
the original and rendered labels, keyword or numeric placement metadata,
the top-left anchor, column positions and widths, row positions, gap,
line spacing, block size, graphical settings, current \code{usr}, and device.
}
\description{
\code{aligned_text()} draws a compact base-graphics text block with three
aligned columns. The columns are named \code{lhs}, \code{mid}, and \code{rhs} for
convenience, but the middle column can contain any text, not only relation
symbols.
}
\details{
Labels are passed through \code{\link[=nice_text]{nice_text()}} before drawing, so TeX-like labels
work on ordinary graphics devices and tikz devices. The block can be placed
either by supplying a numeric top-left anchor \verb{(x, y)} or by supplying a
legend-style keyword such as \code{"bottomright"} or \code{"topleft"}.
}
\examples{
plot(0, 0, type = "n", xlim = c(0, 1), ylim = c(0, 1), las = 1)
aligned_text(
  "bottomright",
  lhs = c("$x$", "$y$", "$\\\\R_{0,\\\\textrm{min}}$"),
  mid = c("$=$", "$=$", "$\\\\simeq$"),
  rhs = c("$0.999$", "$0.001$", "$2$")
)

plot(0, 0, type = "n", xlim = c(0, 1), ylim = c(0, 1), las = 1)
aligned_text(
  0.1, 0.9,
  lhs = c("alpha", "beta"),
  mid = c("is", "is"),
  rhs = c("one", "two")
)

}
