% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{mts_panel_label}
\alias{mts_panel_label}
\title{Add a label to an mts plot panel}
\usage{
mts_panel_label(
  plot.info,
  panel = 1,
  label,
  position = "topleft",
  inset = 0.02,
  cex = 1,
  font = 2,
  col = "black",
  xpd = NA,
  ...
)
}
\arguments{
\item{plot.info}{Plot metadata returned by \code{\link[=mts_plot]{mts_plot()}}.}

\item{panel}{Full-layout panel index. Panel numbering follows the layout
order recorded in \code{plot.info$panels}, including both data panels and
reserved blank panels.}

\item{label}{Single label to draw. Character strings, plotmath expressions,
and labels returned by \code{\link[=nice_text]{nice_text()}} are accepted.}

\item{position}{Label position. Use one of \code{"topleft"}, \code{"topright"},
\code{"bottomleft"}, \code{"bottomright"}, \code{"center"}, or \code{"centre"}, or a numeric
vector \code{c(x, y)} giving user coordinates in the selected panel. Keyword
positions are resolved correctly on linear and log axes.}

\item{inset}{Numeric inset as a fraction of the panel user-coordinate range.
A scalar is used for both directions; a length-two vector is interpreted
as x and y inset.}

\item{cex, font, col}{Text graphical parameters passed to \code{\link[graphics:text]{graphics::text()}}.}

\item{xpd}{Clipping control. The previous \code{xpd} value is restored on exit.}

\item{...}{Additional arguments passed to \code{\link[graphics:text]{graphics::text()}}.}
}
\value{
Invisibly returns \code{plot.info}.
}
\description{
Draw a single text label in one panel created by \code{\link[=mts_plot]{mts_plot()}}. This helper
adds to an existing panel without clearing it, unlike \code{\link[=mts_set_panel]{mts_set_panel()}}.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
plot.info <- mts_plot(x)
mts_panel_label(plot.info, panel = 1, label = "A")

}
