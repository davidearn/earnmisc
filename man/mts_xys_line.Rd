% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{mts_xys_line}
\alias{mts_xys_line}
\title{Add point/slope reference lines to mts plot panels}
\usage{
mts_xys_line(
  x,
  y,
  slope,
  plot.info = NULL,
  panels = NULL,
  columns = NULL,
  source = "xys_line",
  record = TRUE,
  col = NULL,
  lty = NULL,
  lwd = NULL,
  ...
)
}
\arguments{
\item{x, y, slope}{Numeric vectors defining points and slopes. All
combinations of \code{x}, \code{y}, and \code{slope} are drawn in each selected panel.
Infinite slopes draw vertical lines through \code{x}.}

\item{plot.info}{Plot metadata returned by \code{\link[=mts_plot]{mts_plot()}}. If \code{NULL}, the most
recent \code{mts_plot()} result is used.}

\item{panels}{Optional full layout panel indices. If supplied, these panels
are used directly and may include blank panels.}

\item{columns}{Optional plotted data columns. Character values are matched to
plotted column names; numeric values are interpreted as positions among the
plotted columns.}

\item{source}{Source label used when recording the reference lines. It may be
a non-empty character scalar or scalar expression-like label, including
output from \code{\link[=nice_text]{nice_text()}}.}

\item{record}{Logical. If \code{TRUE}, append point/slope rows to the
\code{plot.info$curves} registry for later legend construction.}

\item{col, lty, lwd}{Common graphical arguments passed to
\code{\link[graphics:abline]{graphics::abline()}} through \code{\link[=xys_line]{xys_line()}}. They may be scalar, vectorised
over selected panels and generated lines, or named by plotted column where
possible.}

\item{...}{Graphical arguments passed to \code{\link[graphics:abline]{graphics::abline()}} through
\code{\link[=xys_line]{xys_line()}}.}
}
\value{
Invisibly returns the updated \code{earnmisc_mts_plot_info} object.
}
\description{
Add point/slope reference lines to panels created by \code{\link[=mts_plot]{mts_plot()}}. This is
analogous to \code{\link[=mts_abline]{mts_abline()}}, but uses the default numeric \code{\link[=xys_line]{xys_line()}}
convention: finite slopes draw \code{y - slope * x} intercept lines and infinite
slopes draw vertical lines through \code{x}. This helper is part of the
\code{mts_plot()} workflow and is not intended for arbitrary existing
\code{plot.mts()} output. By default, it draws on all data panels and skips
reserved blank panels.
}
\examples{
z <- stats::ts(cbind(a = 1:10, b = 11:20))
plot.info <- mts_plot(z)
plot.info <- mts_xys_line(0, 5, 1, plot.info = plot.info, col = "grey50")
plot.info <- mts_xys_line(
  0,
  c(0.1, -0.1),
  1,
  plot.info = plot.info,
  col = c("blue", "red")
)

plot.info <- mts_plot(z, blank.panels = 1)
plot.info <- mts_xys_line(0, 0, 1, plot.info = plot.info)

}
