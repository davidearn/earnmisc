% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis_labels.R
\name{axis_labels}
\alias{axis_labels}
\title{Place labels near axis ends}
\usage{
axis_labels(
  xlab = NULL,
  ylab = NULL,
  x.pos = c("right", "left", "centre", "center", "end"),
  y.pos = c("top", "bottom", "centre", "center", "end"),
  x.at = NULL,
  y.at = NULL,
  x.frac = 0.96,
  y.frac = 0.96,
  x.end.offset = 0.02,
  y.end.offset = 0.02,
  line = graphics::par("mgp")[2],
  cex = 1.5,
  las = 1,
  col = graphics::par("col.axis"),
  use.tikz = NULL,
  ...
)
}
\arguments{
\item{xlab, ylab}{Optional character scalar labels for the horizontal and
vertical axes. Non-\code{NULL} labels are passed through \code{\link[=nice_text]{nice_text()}} before
drawing, so TeX-like labels work in ordinary and tikz graphics contexts.}

\item{x.pos}{Position for \code{xlab}. \code{"right"} and \code{"left"} place the label
along the bottom tick-label margin near the corresponding end of the axis.
\code{"centre"} and \code{"center"} place it at the centre. \code{"end"} places it just
inside the right end of the x-axis using \code{\link[graphics:text]{graphics::text()}}.}

\item{y.pos}{Position for \code{ylab}. \code{"top"} and \code{"bottom"} place the label
along the left tick-label margin near the corresponding end of the axis.
\code{"centre"} and \code{"center"} place it at the centre. \code{"end"} places it just
inside the top end of the y-axis using \code{\link[graphics:text]{graphics::text()}}.}

\item{x.at, y.at}{Optional explicit user-coordinate positions. \code{x.at}
overrides the fractional horizontal position for \code{xlab}; \code{y.at} overrides
the fractional vertical position for \code{ylab}.}

\item{x.frac, y.frac}{Fractions of the current plotting range used for
along-axis placement. For example, \code{x.pos = "right"} uses \code{x.frac}, while
\code{x.pos = "left"} uses \code{1 - x.frac}.}

\item{x.end.offset, y.end.offset}{Fractions of the current plotting range used
to move axis-end labels just inside the plot region. This avoids clipping
and avoids requiring extra margins.}

\item{line}{Margin line passed to \code{\link[graphics:mtext]{graphics::mtext()}} for along-axis labels.
The default \code{par("mgp")[2]} aligns labels with numerical tick labels.}

\item{cex}{Text size passed to \code{\link[graphics:mtext]{graphics::mtext()}} and \code{\link[graphics:text]{graphics::text()}}.}

\item{las}{Text orientation passed to \code{\link[graphics:mtext]{graphics::mtext()}}. The default
\code{las = 1} draws horizontal along-axis labels.}

\item{col}{Text colour passed to \code{\link[graphics:mtext]{graphics::mtext()}} and \code{\link[graphics:text]{graphics::text()}}.}

\item{use.tikz}{Optional logical scalar passed to \code{\link[=nice_text]{nice_text()}}. If \code{NULL},
\code{\link[=nice_text]{nice_text()}} resolves tikz mode from the caller or active graphics device.}

\item{...}{Additional graphical arguments passed to \code{\link[graphics:mtext]{graphics::mtext()}} and
\code{\link[graphics:text]{graphics::text()}}, such as \code{font}, \code{adj}, or \code{xpd}.}
}
\value{
Invisibly returns an \code{earnmisc_axis_labels_info} list containing the
original and rendered labels, resolved positions, drawing method for each
label, graphical settings, current \code{usr}, and log-axis state.
}
\description{
Add TeX-aware axis labels to the current base graphics plot without relying
on the usual centred \code{xlab} and \code{ylab} margins. Along-axis labels are drawn
with \code{\link[graphics:mtext]{graphics::mtext()}} on the same line as numerical tick labels. Axis-end
labels are drawn with \code{\link[graphics:text]{graphics::text()}} just inside the plotting region near
the right or top end of an axis.
}
\details{
\code{axis_labels()} is intended to be called after a plot has already been drawn,
usually with \code{xlab = ""}, \code{ylab = ""}, and \code{las = 1}.

For \code{x.pos = "right"}, \code{"left"}, \code{"centre"}, and \code{"center"}, the horizontal
label is drawn on side 1 with \code{\link[graphics:mtext]{graphics::mtext()}}. For \code{y.pos = "top"},
\code{"bottom"}, \code{"centre"}, and \code{"center"}, the vertical label is drawn on side 2
with \code{\link[graphics:mtext]{graphics::mtext()}}. These labels use \code{line = par("mgp")[2]} by default,
matching the tick-label line and avoiding extra margin requirements.

For \code{x.pos = "end"} and \code{y.pos = "end"}, labels are drawn with
\code{\link[graphics:text]{graphics::text()}} inside the plot region. The x-axis end label is placed
near the lower-right corner; the y-axis end label is placed near the
upper-left corner. The offsets are measured as fractions of the visible plot
range in the current graphics coordinate system. On log axes, fractions are
computed on the transformed axis scale and then converted back to ordinary
user coordinates for drawing.
}
\examples{
x <- seq(0, 1, length.out = 100)
plot(x, x * (1 - x), xlab = "", ylab = "", las = 1)
axis_labels("$x$", "$y$", x.pos = "right", y.pos = "top")

plot(x, sin(2 * pi * x), xlab = "", ylab = "", las = 1)
axis_labels("$\\\\tau$", "$\\\\iota$", x.pos = "end", y.pos = "end")

}
