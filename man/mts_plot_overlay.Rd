% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{mts_plot_overlay}
\alias{mts_plot_overlay}
\title{Plot an mts object with one or more overlays}
\usage{
mts_plot_overlay(
  x,
  ...,
  columns.x = NULL,
  columns.y = NULL,
  match = c("name", "position"),
  unmatched = c("warn", "error", "ignore"),
  col.x = "black",
  lty.x = 1,
  lwd.x = 1,
  col.y = NULL,
  lty.y = NULL,
  lwd.y = NULL,
  source.x = NULL,
  overlay.names = NULL,
  plot.args = list(),
  lines.args = list()
)
}
\arguments{
\item{x}{Base multivariate time-series object.}

\item{...}{One or more overlay multivariate time-series objects.}

\item{columns.x}{Optional base columns.}

\item{columns.y}{Optional overlay columns used for each overlay object.}

\item{match, unmatched}{Matching and unmatched-column behaviour passed to
\code{\link[=mts_lines]{mts_lines()}}.}

\item{col.x, lty.x, lwd.x}{Base graphical parameters.}

\item{col.y, lty.y, lwd.y}{Overlay graphical parameters. If \code{NULL}, simple
defaults are used. If a list, each element is used for the corresponding
overlay object; otherwise the value is reused for each overlay object.}

\item{source.x}{Optional source label for the base object. It may be a
non-empty character scalar or scalar expression-like label. If \code{NULL}, a
label is inferred from the expression supplied for \code{x}.}

\item{overlay.names}{Optional source labels for overlay objects. A character
vector or expression vector must contain one label per overlay object. If
\code{NULL}, labels are inferred from the expressions supplied in \code{...}.}

\item{plot.args}{Additional arguments passed to \code{\link[=mts_plot]{mts_plot()}}. Same-named
values override base plotting defaults such as \code{col.x}, \code{lty.x}, and
\code{lwd.x}. Core arguments \code{x}, \code{columns}, and \code{source} cannot be supplied
here.}

\item{lines.args}{Additional arguments passed to \code{\link[=mts_lines]{mts_lines()}}. Same-named
values override overlay defaults such as \code{col.y}, \code{lty.y}, and \code{lwd.y}.
Core arguments \code{y}, \code{plot.info}, \code{columns}, \code{source}, and \code{object.index}
cannot be supplied here.}
}
\value{
Invisibly returns the final updated plot-info object.
}
\description{
Convenience wrapper that calls \code{\link[=mts_plot]{mts_plot()}} once and \code{\link[=mts_lines]{mts_lines()}} once for
each overlay object supplied in \code{...}. Source labels are inferred from the
input expressions unless \code{source.x} or \code{overlay.names} are supplied.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
y <- stats::ts(cbind(a = 2:11, b = 10:19))
z <- stats::ts(cbind(a = 3:12, b = 9:18))
plot.info <- mts_plot_overlay(x, y)
plot.info <- mts_plot_overlay(x, y, z)
plot.info$curves
plot.info <- mts_plot_overlay(
  x, y, z,
  source.x = "baseline",
  overlay.names = c("comparison 1", "comparison 2")
)

}
