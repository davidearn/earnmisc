% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{legend_mts}
\alias{legend_mts}
\title{Add a legend to an mts plot panel}
\usage{
legend_mts(
  plot.info = NULL,
  panel = NULL,
  by = c("source", "curve", "column"),
  legend = NULL,
  x = "center",
  inset = 0,
  bty = "n",
  ...
)
}
\arguments{
\item{plot.info}{Plot metadata returned by \code{\link[=plot_mts]{plot_mts()}}. If \code{NULL}, the most
recent \code{plot_mts()} result is used.}

\item{panel}{Optional full layout panel index. If \code{NULL}, the first blank
panel in \code{plot.info$blank.panels} is used.}

\item{by}{Legend grouping: \code{"source"}, \code{"curve"}, or \code{"column"}.}

\item{legend}{Optional legend labels. If \code{NULL}, labels are constructed from
the selected curve grouping.}

\item{x, inset, bty}{Arguments passed to \code{\link[graphics:legend]{graphics::legend()}}.}

\item{...}{Additional arguments passed to \code{\link[graphics:legend]{graphics::legend()}}. Same-named
values such as \code{col}, \code{lty}, or \code{lwd} override the defaults constructed
from \code{plot.info$curves}.}
}
\value{
Invisibly returns a list containing the legend panel, grouping,
labels, graphical parameters, selected curves, and the result from
\code{\link[graphics:legend]{graphics::legend()}}.
}
\description{
Draw a legend for curves recorded by \code{\link[=plot_mts]{plot_mts()}} and \code{\link[=lines_mts]{lines_mts()}}. By
default, the legend is grouped by source label and drawn in the first panel
reserved with \code{blank.panels}; otherwise supply \code{panel} explicitly. For
\code{by = "source"}, grouping uses the character \code{source} key, while labels come
from the preserved \code{source.label} values so plotmath and \code{\link[=nice_text]{nice_text()}} labels
can be rendered by \code{\link[graphics:legend]{graphics::legend()}}. Explicit \code{legend} labels override
source-derived labels.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20, c = 21:30))
y <- stats::ts(cbind(a = 2:11, b = 10:19, c = 20:29))
plot.info <- plot_mts(x, blank.panels = 1)
plot.info <- lines_mts(y, plot.info = plot.info, source = "overlay")
legend_mts(plot.info)

}
