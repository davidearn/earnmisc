% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{lines_mts}
\alias{lines_mts}
\title{Overlay multivariate time-series columns on \code{plot_mts()} panels}
\usage{
lines_mts(
  y,
  plot.info = NULL,
  columns = NULL,
  match = c("name", "position"),
  unmatched = c("warn", "error", "ignore"),
  col = "red",
  lty = 1,
  lwd = 1,
  source = NULL,
  object.index = NULL,
  ...
)
}
\arguments{
\item{y}{Overlay multivariate time-series object.}

\item{plot.info}{Plot metadata returned by \code{\link[=plot_mts]{plot_mts()}}. If \code{NULL}, the most
recent \code{plot_mts()} result is used.}

\item{columns}{Optional overlay column names or indices.}

\item{match}{Column matching mode: \code{"name"} or \code{"position"}.}

\item{unmatched}{Behaviour for unmatched overlay columns: \code{"warn"},
\code{"error"}, or \code{"ignore"}.}

\item{col, lty, lwd}{Overlay graphical parameters. They may be scalar,
vectorised by selected overlay column, or named by overlay column.}

\item{source}{Optional curve source label. If \code{NULL}, a label is inferred
from the expression supplied for \code{y}. Repeated direct calls are therefore
tracked separately when they use different input expressions.}

\item{object.index}{Optional overlay object index for the curve registry.}

\item{...}{Additional arguments passed to \code{\link[graphics:lines]{graphics::lines()}}.}
}
\value{
Invisibly returns the updated \code{plot.info} object with appended curve
registry rows.
}
\description{
Overlay selected columns from an \code{mts}-like object onto panels created by
\code{\link[=plot_mts]{plot_mts()}}. This function is not intended for arbitrary existing
\code{plot.mts()} output. When \code{\link[=plot_mts]{plot_mts()}} has reserved blank panels,
\code{lines_mts()} overlays on the correct data panels and skips reserved panels.
If \code{source} is \code{NULL}, the source label is inferred from the expression
supplied for \code{y}, so repeated direct calls with different inputs remain
distinct in \code{\link[=legend_mts]{legend_mts()}} when \code{by = "source"}.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
y <- stats::ts(cbind(b = 10:19, a = 2:11))
plot.info <- plot_mts(x)
plot.info <- lines_mts(y, plot.info = plot.info)

}
