% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{plot_mts_overlay}
\alias{plot_mts_overlay}
\title{Plot an mts object with one or more overlays}
\usage{
plot_mts_overlay(
  x,
  ...,
  columns.x = NULL,
  columns.y = NULL,
  match = c("name", "position"),
  unmatched = c("warn", "error", "ignore"),
  col.x = "black",
  lty.x = 1,
  lwd.x = 1,
  col.y = NULL,
  lty.y = NULL,
  lwd.y = NULL,
  overlay.names = NULL,
  plot.args = list(),
  lines.args = list()
)
}
\arguments{
\item{x}{Base multivariate time-series object.}

\item{...}{One or more overlay multivariate time-series objects.}

\item{columns.x}{Optional base columns.}

\item{columns.y}{Optional overlay columns used for each overlay object.}

\item{match, unmatched}{Matching and unmatched-column behaviour passed to
\code{\link[=lines_mts]{lines_mts()}}.}

\item{col.x, lty.x, lwd.x}{Base graphical parameters.}

\item{col.y, lty.y, lwd.y}{Overlay graphical parameters. If \code{NULL}, simple
defaults are used. If a list, each element is used for the corresponding
overlay object; otherwise the value is reused for each overlay object.}

\item{overlay.names}{Optional source labels for overlay objects.}

\item{plot.args}{Additional arguments passed to \code{\link[=plot_mts]{plot_mts()}}. Same-named
values override base plotting defaults such as \code{col.x}, \code{lty.x}, and
\code{lwd.x}. Core arguments \code{x} and \code{columns} cannot be supplied here.}

\item{lines.args}{Additional arguments passed to \code{\link[=lines_mts]{lines_mts()}}. Same-named
values override overlay defaults such as \code{col.y}, \code{lty.y}, and \code{lwd.y}.
Core arguments \code{y}, \code{plot.info}, \code{columns}, \code{source}, and \code{object.index}
cannot be supplied here.}
}
\value{
Invisibly returns the final updated plot-info object.
}
\description{
Convenience wrapper that calls \code{\link[=plot_mts]{plot_mts()}} once and \code{\link[=lines_mts]{lines_mts()}} once for
each overlay object supplied in \code{...}.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
y <- stats::ts(cbind(a = 2:11, b = 10:19))
z <- stats::ts(cbind(a = 3:12, b = 9:18))
plot.info <- plot_mts_overlay(x, y)
plot.info <- plot_mts_overlay(x, y, z)
plot.info$curves

}
