% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_list.R, R/mts_plot.R
\name{mts_lines}
\alias{mts_lines}
\alias{mts_lines.mts_list}
\alias{mts_lines.default}
\title{Overlay multivariate time-series columns on \code{mts_plot()} panels}
\usage{
\method{mts_lines}{mts_list}(
  y,
  plot.info = NULL,
  columns = NULL,
  match = c("position", "name"),
  unmatched = c("error", "warn", "ignore"),
  col = "red",
  lty = 1,
  lwd = 1,
  type = "l",
  source = NULL,
  object.index = NULL,
  ...
)

mts_lines(
  y,
  plot.info = NULL,
  columns = NULL,
  match = c("name", "position"),
  unmatched = c("warn", "error", "ignore"),
  col = "red",
  lty = 1,
  lwd = 1,
  type = "l",
  source = NULL,
  object.index = NULL,
  ...
)

\method{mts_lines}{default}(
  y,
  plot.info = NULL,
  columns = NULL,
  match = c("name", "position"),
  unmatched = c("warn", "error", "ignore"),
  col = "red",
  lty = 1,
  lwd = 1,
  type = "l",
  source = NULL,
  object.index = NULL,
  ...
)
}
\arguments{
\item{y}{Overlay multivariate time-series object.}

\item{plot.info}{Plot metadata returned by \code{\link[=mts_plot]{mts_plot()}}. If \code{NULL}, the most
recent \code{mts_plot()} result is used.}

\item{columns}{Optional overlay column names or indices.}

\item{match}{Column matching mode: \code{"name"} or \code{"position"}.}

\item{unmatched}{Behaviour for unmatched overlay columns: \code{"warn"},
\code{"error"}, or \code{"ignore"}.}

\item{col, lty, lwd, type}{Overlay graphical parameters. \code{col}, \code{lty}, and
\code{lwd} may be scalar, vectorised by selected overlay column, or named by
overlay column.}

\item{source}{Optional curve source label. It may be a non-empty character
scalar or a scalar expression-like label, including output from
\code{\link[=nice_text]{nice_text()}}. If \code{NULL}, a label is inferred from the expression supplied
for \code{y}.}

\item{object.index}{Optional overlay object index for the curve registry.}

\item{...}{Additional arguments passed to \code{\link[graphics:lines]{graphics::lines()}}.}
}
\value{
Invisibly returns the updated \code{plot.info} object with appended curve
registry rows.
}
\description{
Overlay selected columns from an \code{mts}-like object onto panels created by
\code{\link[=mts_plot]{mts_plot()}}. This function is not intended for arbitrary existing
\code{plot.mts()} output. When \code{\link[=mts_plot]{mts_plot()}} has reserved blank panels,
\code{mts_lines()} overlays on the correct data panels and skips reserved panels.
If \code{source} is \code{NULL}, the source label is inferred from the expression
supplied for \code{y}, so repeated direct calls with different inputs remain
distinct in \code{\link[=mts_legend]{mts_legend()}} when \code{by = "source"}.
For \code{mts_list} objects, \code{\link[=mts_lines]{mts_lines()}} overlays one native-grid series onto
each plotted data panel. The \code{mts_list} method matches panels by position by
default; set \code{match = "name"} to match by panel/series name.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
y <- stats::ts(cbind(b = 10:19, a = 2:11))
plot.info <- mts_plot(x)
plot.info <- mts_lines(y, plot.info = plot.info)

}
