% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{mts_panel_legend}
\alias{mts_panel_legend}
\title{Add a legend to an mts plot panel}
\usage{
mts_panel_legend(
  plot.info,
  panel = 1,
  labels,
  position = "topright",
  inset = 0.02,
  cex = 0.9,
  bty = "n",
  xpd = NA,
  ...
)
}
\arguments{
\item{plot.info}{Plot metadata returned by \code{\link[=mts_plot]{mts_plot()}}.}

\item{panel}{Full-layout panel index. Panel numbering follows the layout
order recorded in \code{plot.info$panels}, including both data panels and
reserved blank panels.}

\item{labels}{Legend labels. Any positive-length vector accepted by
\code{\link[graphics:legend]{graphics::legend()}} can be used, including expression-like labels.}

\item{position}{Legend position. Use one of \code{"topleft"}, \code{"topright"},
\code{"bottomleft"}, \code{"bottomright"}, \code{"center"}, or \code{"centre"}, or a numeric
vector \code{c(x, y)} giving user coordinates in the selected panel.}

\item{inset, cex, bty}{Arguments passed to \code{\link[graphics:legend]{graphics::legend()}}.}

\item{xpd}{Clipping control. The previous \code{xpd} value is restored on exit.}

\item{...}{Additional arguments passed to \code{\link[graphics:legend]{graphics::legend()}}, including
vectorised style arguments such as \code{col}, \code{lty}, \code{lwd}, and \code{pch}.}
}
\value{
Invisibly returns \code{plot.info}.
}
\description{
Draw a simple user-specified legend in one panel created by \code{\link[=mts_plot]{mts_plot()}}.
Unlike \code{\link[=mts_legend]{mts_legend()}}, this helper does not inspect the curve registry or
choose a reserved blank panel. It is intended for explicit panel annotations
where the caller already knows the legend labels and graphical styles.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
plot.info <- mts_plot(x)
mts_panel_legend(
  plot.info,
  panel = 1,
  labels = c("exact", "global"),
  col = c("grey60", "black"),
  lty = c(1, 2),
  lwd = c(3, 2)
)

}
