% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{mts_set_panel}
\alias{mts_set_panel}
\title{Reinitialise an mts plot panel}
\usage{
mts_set_panel(
  panel,
  plot.info = NULL,
  xlim = c(0, 1),
  ylim = c(0, 1),
  axes = FALSE,
  xaxs = "i",
  yaxs = "i",
  ...
)
}
\arguments{
\item{panel}{Full layout panel index.}

\item{plot.info}{Plot metadata returned by \code{\link[=mts_plot]{mts_plot()}}. If \code{NULL}, the most
recent \code{mts_plot()} result is used.}

\item{xlim, ylim}{Coordinate limits passed to \code{\link[graphics:plot.window]{graphics::plot.window()}}.}

\item{axes}{Logical value. If \code{TRUE}, simple axes and a box are drawn after
the plot window is initialised.}

\item{xaxs, yaxs}{Axis style arguments passed to \code{\link[graphics:plot.window]{graphics::plot.window()}}.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot.window]{graphics::plot.window()}}.}
}
\value{
Invisibly returns metadata for the selected panel.
}
\description{
Select a panel from a layout created by \code{\link[=mts_plot]{mts_plot()}}, clear it with
\code{\link[graphics:frame]{graphics::plot.new()}}, and initialise a simple coordinate system with
\code{\link[graphics:plot.window]{graphics::plot.window()}}. This is intended mainly for reserved blank panels
used for legends, notes, or later custom graphics. It clears the selected
panel, so it is not intended for adding annotations on top of an already
drawn data panel; a future \code{mts_add_to_panel()} helper may support that.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20, c = 21:30))
plot.info <- mts_plot(x, blank.panels = c(1, 4))
mts_set_panel(4, plot.info)
graphics::text(0.5, 0.5, "Notes")

}
