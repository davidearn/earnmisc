% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{mts_plot}
\alias{mts_plot}
\alias{mts_plot.default}
\title{Plot a multivariate time series in panels}
\usage{
mts_plot(
  x,
  columns = NULL,
  panel = c("separate", "overlay"),
  nrow = NULL,
  ncol = NULL,
  panel.fill = c("row", "column"),
  blank.panels = NULL,
  panel.main = TRUE,
  legend = FALSE,
  main = NULL,
  xlab = "Time",
  ylab = NULL,
  col = "black",
  lty = 1,
  lwd = 1,
  type = "l",
  source = NULL,
  axes = TRUE,
  frame.plot = TRUE,
  bty = graphics::par("bty"),
  mar = NULL,
  oma = NULL,
  las = 1,
  xlim = NULL,
  ylim = NULL,
  ...
)

\method{mts_plot}{default}(
  x,
  columns = NULL,
  panel = c("separate", "overlay"),
  nrow = NULL,
  ncol = NULL,
  panel.fill = c("row", "column"),
  blank.panels = NULL,
  panel.main = TRUE,
  legend = FALSE,
  main = NULL,
  xlab = "Time",
  ylab = NULL,
  col = "black",
  lty = 1,
  lwd = 1,
  type = "l",
  source = NULL,
  axes = TRUE,
  frame.plot = TRUE,
  bty = graphics::par("bty"),
  mar = NULL,
  oma = NULL,
  las = 1,
  xlim = NULL,
  ylim = NULL,
  ...
)
}
\arguments{
\item{x}{Multivariate time-series object, or an object safely coercible to a
matrix with a regular sequence time index.}

\item{columns}{Optional column names or indices to plot.}

\item{panel}{Panel mode. \code{"separate"} draws one panel per selected series.
\code{"overlay"} draws all selected series on one panel for classes that
support overlay-panel plotting.}

\item{nrow, ncol}{Optional panel layout dimensions.}

\item{panel.fill}{Panel filling order for multi-panel plots. \code{"row"} fills
across rows, equivalent to \code{\link[graphics:par]{graphics::par()}} \code{mfrow}; \code{"column"} fills
down columns, equivalent to \code{mfcol}.}

\item{blank.panels}{Optional positive integer vector of full layout panel
indices to reserve for later legends, annotations, or custom graphics.
Reserved panels are blank after \code{mts_plot()} finishes.}

\item{panel.main}{Logical scalar. If \code{TRUE}, separate-panel methods use
column names as panel main titles. If \code{FALSE}, separate panels have no
main titles.}

\item{legend}{Simple legend control for overlay-panel methods.}

\item{main}{Optional outer title.}

\item{xlab}{X-axis label.}

\item{ylab}{Optional y-axis label. If \code{NULL}, each panel uses its column name.}

\item{col, lty, lwd, type}{Graphical parameters for base curves. \code{col}, \code{lty},
and \code{lwd} may be scalar, vectorised by selected column, or named by column.}

\item{source}{Optional source label for the base curves. It may be a
non-empty character scalar or a scalar expression-like label, including
output from \code{\link[=nice_text]{nice_text()}}. If \code{NULL}, a label is inferred from the
expression supplied for \code{x}.}

\item{axes, frame.plot}{Logical values passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}

\item{bty}{Box type passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}

\item{mar, oma}{Optional margin settings passed to \code{\link[graphics:par]{graphics::par()}}.}

\item{las}{Axis-label orientation passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}

\item{xlim, ylim}{Optional axis limits.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}
}
\value{
Invisibly returns an \code{earnmisc_mts_plot_info} list containing panel
metadata and a \code{curves} registry. The registry is used by \code{\link[=mts_legend]{mts_legend()}}
and can also be inspected directly.
}
\description{
Plot selected columns of a multivariate time series, one column per panel,
and return metadata that \code{\link[=mts_lines]{mts_lines()}}, \code{\link[=mts_abline]{mts_abline()}}, and
\code{\link[=mts_xys_line]{mts_xys_line()}} can use for later overlays.
}
\details{
These helpers provide a coherent base-graphics workflow for \code{mts} overlays:
call \code{mts_plot()} first, then pass the returned object to \code{mts_lines()}.
Overlaying onto arbitrary existing \code{\link[stats:plot.ts]{stats::plot.ts()}} or \code{plot.mts()} output
is not supported because base R does not expose a stable public panel map.
Curve sources are recorded in the \code{curves} registry. The \code{source} column is
a character key used for grouping and inspection. The \code{source.label}
list-column preserves the display label, including expression-like labels
used by \code{\link[graphics:legend]{graphics::legend()}}.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
y <- stats::ts(cbind(a = 2:11, b = 10:19))
plot.info <- mts_plot(x)
plot.info <- mts_lines(y, plot.info = plot.info)
plot.info$curves
plot.info <- mts_plot(x, source = "baseline")
plot.info <- mts_lines(y, plot.info = plot.info, source = "comparison")
mts_legend(plot.info, by = "source", panel = 1)
plot.info <- mts_plot(x, source = expression(R[0] == 8))
plot.info <- mts_lines(y, plot.info = plot.info, source = expression(R[0] == 4))
mts_legend(plot.info, by = "source", panel = 1)

x3 <- stats::ts(cbind(a = 1:10, b = 11:20, c = 21:30))
y3 <- stats::ts(cbind(a = 2:11, b = 10:19, c = 20:29))
plot.info <- mts_plot(x3, blank.panels = 1)
plot.info <- mts_lines(y3, plot.info = plot.info, source = "overlay")
mts_legend(plot.info)

plot.info <- mts_plot(x3, blank.panels = c(1, 4))
mts_set_panel(4, plot.info)
graphics::text(0.5, 0.5, "Notes")

}
