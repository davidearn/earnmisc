% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_plot.R
\name{plot_mts}
\alias{plot_mts}
\title{Plot a multivariate time series in panels}
\usage{
plot_mts(
  x,
  columns = NULL,
  nrow = NULL,
  ncol = NULL,
  main = NULL,
  xlab = "Time",
  ylab = NULL,
  col = "black",
  lty = 1,
  lwd = 1,
  type = "l",
  axes = TRUE,
  frame.plot = TRUE,
  mar = NULL,
  oma = NULL,
  ...
)
}
\arguments{
\item{x}{Multivariate time-series object, or an object safely coercible to a
matrix with a regular sequence time index.}

\item{columns}{Optional column names or indices to plot.}

\item{nrow, ncol}{Optional panel layout dimensions.}

\item{main}{Optional outer title.}

\item{xlab}{X-axis label.}

\item{ylab}{Optional y-axis label. If \code{NULL}, each panel uses its column name.}

\item{col, lty, lwd, type}{Graphical parameters for base curves. \code{col}, \code{lty},
and \code{lwd} may be scalar, vectorised by selected column, or named by column.}

\item{axes, frame.plot}{Logical values passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}

\item{mar, oma}{Optional margin settings passed to \code{\link[graphics:par]{graphics::par()}}.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}
}
\value{
Invisibly returns an \code{earnmisc_mts_plot_info} list containing panel
metadata and a \code{curves} registry.
}
\description{
Plot selected columns of a multivariate time series, one column per panel,
and return metadata that \code{\link[=lines_mts]{lines_mts()}} can use for later overlays.
}
\details{
These helpers provide a coherent base-graphics workflow for \code{mts} overlays:
call \code{plot_mts()} first, then pass the returned object to \code{lines_mts()}.
Overlaying onto arbitrary existing \code{\link[stats:plot.ts]{stats::plot.ts()}} or \code{plot.mts()} output
is not supported because base R does not expose a stable public panel map.
}
\examples{
x <- stats::ts(cbind(a = 1:10, b = 11:20))
y <- stats::ts(cbind(a = 2:11, b = 10:19))
plot.info <- plot_mts(x)
plot.info <- lines_mts(y, plot.info = plot.info)
plot.info$curves

}
