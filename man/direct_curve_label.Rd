% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct_curve_label.R
\name{direct_curve_label}
\alias{direct_curve_label}
\title{Label a plotted curve directly}
\usage{
direct_curve_label(
  x,
  y,
  label,
  placement = c("x_offset", "peak_fraction"),
  x.offset = 0.05,
  peak.fraction = 0.7,
  x.text.offset = 0,
  y.text.offset = 0,
  adj = c(0, 0.5),
  col = "black",
  cex = 0.8,
  font = 1,
  xpd = NA,
  ...
)
}
\arguments{
\item{x, y}{Finite numeric vectors giving the curve coordinates.}

\item{label}{Single label to draw. Plain character strings, plotmath
expressions, and labels already converted by \code{\link[=nice_text]{nice_text()}} are accepted.}

\item{placement}{Placement rule. \code{"x_offset"} chooses a point after the
peak nearest \code{peak.x + x.offset * panel_width}. \code{"peak_fraction"} chooses
the first descending-side point where the curve has fallen to
\code{peak.fraction * peak.y}, using linear interpolation when possible.}

\item{x.offset}{Non-negative horizontal offset, as a fraction of the current
panel width, used by \code{placement = "x_offset"}. On a log x-axis this is
interpreted on the displayed log scale.}

\item{peak.fraction}{Peak-height fraction used by
\code{placement = "peak_fraction"}. Values must satisfy
\verb{0 < peak.fraction <= 1}. When \code{peak.fraction = 1}, the peak point itself
is used.}

\item{x.text.offset, y.text.offset}{Text offsets applied after the curve
point is selected, as fractions of the current panel width and height.
Offsets are interpreted on the displayed scale for log axes.}

\item{adj, col, cex, font, xpd}{Graphical parameters passed to
\code{\link[graphics:text]{graphics::text()}}. The previous \code{xpd} value is restored on exit.}

\item{...}{Additional arguments passed to \code{\link[graphics:text]{graphics::text()}}.}
}
\value{
Invisibly returns the selected text location as a numeric vector
\code{c(x = ..., y = ...)}.
}
\description{
\code{direct_curve_label()} places a text label on or near one already-plotted
curve. It uses a simple peak-based rule to choose a point on the descending
side of the curve, then draws \code{\link[graphics:text]{graphics::text()}} at that point with optional
offsets. The helper is generic base-graphics code; it does not depend on
\code{mts_plot()} metadata.
}
\examples{
x <- seq(-3, 3, length.out = 200)
y <- exp(-x^2)
plot(x, y, type = "l")
direct_curve_label(x, y, label = "curve")

}
