% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_pp_plot.R
\name{mts_pp_plot}
\alias{mts_pp_plot}
\alias{pp_plot.mts}
\title{Plot phase-plane panels for mts-like data}
\usage{
mts_pp_plot(
  x,
  h.var = NULL,
  v.var = NULL,
  pairs = NULL,
  max.panels = 16,
  label.map = NULL,
  use.tikz = NULL,
  xlim = NULL,
  ylim = NULL,
  col = "black",
  lty = 1,
  lwd = 1,
  type = "l",
  axes = TRUE,
  frame.plot = TRUE,
  ...
)

\method{pp_plot}{mts}(x, ...)
}
\arguments{
\item{x}{Multivariate time-series object, or an object safely coercible to a
numeric matrix.}

\item{h.var}{Horizontal variable names or indices. If \code{h.var} and \code{v.var} are
both supplied, all non-identical Cartesian pairs are plotted.}

\item{v.var}{Vertical variable names or indices. If \code{h.var} and \code{v.var} are
both supplied, all non-identical Cartesian pairs are plotted.}

\item{pairs}{Explicit phase-plane pairs. This overrides \code{h.var} and \code{v.var}.
Supply either a two-column matrix/data frame, preferably with columns
\code{h.var} and \code{v.var}, or a list whose elements are length-two pairs. Pair
entries may be column names or column indices.}

\item{max.panels}{Positive integer scalar giving the maximum number of panels
to draw. If the requested pairs exceed this value, the function stops before
plotting and reports how to raise the limit or restrict the pairs.}

\item{label.map}{Optional named character vector or named list mapping column
names to display labels. Labels are passed through \code{\link[=nice_text]{nice_text()}} so TeX-like
labels can be used in ordinary and tikz graphics contexts.}

\item{use.tikz}{Optional logical scalar passed to \code{\link[=nice_text]{nice_text()}}. If \code{NULL},
\code{\link[=nice_text]{nice_text()}} resolves the mode from the calling context or active device.}

\item{xlim, ylim}{Optional finite numeric length-two ranges. When supplied,
these override the automatically computed horizontal or vertical axis
ranges for every phase-plane panel.}

\item{col, lty, lwd, type}{Graphical parameters for base trajectories. \code{col},
\code{lty}, and \code{lwd} may be scalar, vectorised by phase-plane pair, or named by
pair name such as \code{"x-y"}.}

\item{axes, frame.plot}{Logical values passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}
}
\value{
Invisibly returns an \code{earnmisc_pp_plot_info} list containing at least
\code{pairs}, \code{layout}, \code{xlim}, \code{ylim}, \code{labels}, \code{panels}, and \code{curves}.
}
\description{
Plot one or more phase-plane panels from a multivariate time series or
matrix-like object. Each panel plots one selected vertical variable against
one selected horizontal variable. The returned plot metadata records the
plotted pairs, panel layout, coordinate ranges, labels, and graphics state so
\code{\link[=mts_pp_lines]{mts_pp_lines()}} can add matching overlays.
}
\details{
If \code{pairs} is supplied, it is used exactly. If both \code{h.var} and \code{v.var} are
supplied, all Cartesian combinations are used except pairs where the
horizontal and vertical variables are identical. If neither \code{pairs} nor
\code{h.var}/\code{v.var} is supplied, all unordered column pairs are used once; for
columns \code{x}, \code{y}, and \code{z}, the default pairs are \code{x-y}, \code{x-z}, and \code{y-z}.

For reliable multi-panel overlays, pass the returned object to
\code{\link[=mts_pp_lines]{mts_pp_lines()}}. This lets overlays reuse the same pair ordering, panel
layout, and coordinate ranges:

\code{info <- mts_pp_plot(x1, h.var = "x", v.var = "y")}
\code{info <- mts_pp_lines(x2, plot.info = info, lty = 2)}
}
\examples{
time <- seq(0, 2 * pi, length.out = 100)
x <- cbind(
  sin = sin(time),
  cos = cos(time),
  decay = exp(-time / 4)
)
x <- stats::ts(x)
info <- mts_pp_plot(x, h.var = "sin", v.var = "cos")
info$pairs

labels <- c(sin = "$s$", cos = "$c$")
mts_pp_plot(x, h.var = "sin", v.var = "cos", label.map = labels)

}
