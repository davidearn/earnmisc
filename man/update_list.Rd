% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_utils.R
\name{update_list}
\alias{update_list}
\title{Update elements of a list}
\usage{
update_list(x, ..., .create = FALSE)
}
\arguments{
\item{x}{A list-like object.}

\item{...}{Named updates. Names identify elements to replace, such as
\code{type} or \code{"parms$graphics$lwd"}.}

\item{.create}{Logical scalar. If \code{TRUE}, create missing intermediate lists.
If \code{FALSE}, missing intermediate path components are errors.}
}
\value{
A modified copy of \code{x}, preserving top-level attributes and class
where possible.
}
\description{
Return a modified copy of a list-like object after replacing selected
elements. Update names can refer to top-level elements or to nested elements
using simple \code{$}-separated paths.
}
\details{
Duplicate update paths are rejected, because repeated replacements for the
same element are usually accidental.
}
\examples{
x <- list(type = "old", parms = list(graphics = list(lwd = 1)))
update_list(x, type = "new")
update_list(x, "parms$graphics$lwd" = 3)
update_list(list(), "parms$graphics$lwd" = 3, .create = TRUE)

}
