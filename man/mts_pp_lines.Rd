% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mts_pp_plot.R
\name{mts_pp_lines}
\alias{mts_pp_lines}
\alias{pp_lines.mts}
\title{Overlay phase-plane trajectories for mts-like data}
\usage{
mts_pp_lines(
  x,
  h.var = NULL,
  v.var = NULL,
  pairs = NULL,
  plot.info = NULL,
  label.map = NULL,
  use.tikz = NULL,
  col = "red",
  lty = 1,
  lwd = 1,
  type = "l",
  ...
)

\method{pp_lines}{mts}(x, ...)
}
\arguments{
\item{x}{Overlay multivariate time-series object, or an object safely
coercible to a numeric matrix.}

\item{h.var, v.var, pairs}{Pair selectors used only when \code{plot.info} is \code{NULL}.
They follow the same rules as \code{\link[=mts_pp_plot]{mts_pp_plot()}}.}

\item{plot.info}{Optional metadata returned by \code{\link[=mts_pp_plot]{mts_pp_plot()}}. If supplied,
overlays use the recorded pairs and panels.}

\item{label.map}{Optional named character vector or named list mapping column
names to display labels. Used only when \code{plot.info} is \code{NULL}.}

\item{use.tikz}{Optional logical scalar passed to \code{\link[=nice_text]{nice_text()}}. Used only
when \code{plot.info} is \code{NULL}.}

\item{col, lty, lwd, type}{Graphical parameters for overlay trajectories. \code{col},
\code{lty}, and \code{lwd} may be scalar, vectorised by phase-plane pair, or named by
pair name such as \code{"x-y"}.}

\item{...}{Additional arguments passed to \code{\link[graphics:lines]{graphics::lines()}}.}
}
\value{
Invisibly returns an updated \code{earnmisc_pp_plot_info} object.
}
\description{
Add one or more phase-plane trajectories to existing panels created by
\code{\link[=mts_pp_plot]{mts_pp_plot()}}. Passing \code{plot.info} is recommended for multi-panel overlays
because it reuses the original pair ordering, panel layout, and coordinate
ranges.
}
\examples{
time <- seq(0, 2 * pi, length.out = 100)
x <- stats::ts(cbind(sin = sin(time), cos = cos(time)))
y <- stats::ts(cbind(sin = 0.9 * sin(time), cos = 0.9 * cos(time)))
info <- mts_pp_plot(x, h.var = "sin", v.var = "cos")
# Overlay a second, smaller trajectory.
info <- mts_pp_lines(y, plot.info = info, col = "red", lty = 2)

}
