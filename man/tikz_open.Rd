% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tikz.R
\name{tikz_open}
\alias{tikz_open}
\title{Open a tikz graphics device and store metadata}
\usage{
tikz_open(
  file = filename,
  filename = ifelse(onefile, "./Rplots.tex", "./Rplot\%03d.tex"),
  width = 7,
  height = 7,
  onefile = TRUE,
  bg = "transparent",
  fg = "black",
  pointsize = 10,
  lwdUnit = getOption("tikzLwdUnit"),
  standAlone = TRUE,
  bareBones = FALSE,
  console = FALSE,
  sanitize = FALSE,
  engine = getOption("tikzDefaultEngine"),
  documentDeclaration = getOption("tikzDocumentDeclaration"),
  packages = NULL,
  footer = getOption("tikzFooter"),
  symbolicColors = getOption("tikzSymbolicColors"),
  colorFileName = "\%s_colors.tex",
  maxSymbolicColors = getOption("tikzMaxSymbolicColors"),
  timestamp = TRUE,
  verbose = interactive(),
  message = TRUE
)
}
\arguments{
\item{file}{Output \code{.tex} file passed to \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}.}

\item{filename}{Default filename pattern passed through to
\code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}.}

\item{width, height}{Width and height of the graphics device in inches.}

\item{onefile, bg, fg, pointsize, lwdUnit, standAlone, bareBones, console, sanitize}{Arguments passed to \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}.}

\item{engine, documentDeclaration, packages, footer, symbolicColors, colorFileName}{Arguments passed to \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}. If \code{packages = NULL}, the
argument is omitted so \code{tikzDevice} can use its own default.}

\item{maxSymbolicColors, timestamp, verbose}{Arguments passed to
\code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}.}

\item{message}{Logical scalar. If \code{TRUE}, print a short message after the
device opens successfully.}
}
\value{
Invisibly returns an object of class \code{earnmisc_tikz_info}, a list
containing the tikz arguments plus metadata including \code{device},
\code{device.name}, \code{opened_at}, \code{working_directory}, \code{normalized_file}, and
\code{pdf_file}.
}
\description{
Open a tikz graphics device with \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}} and store useful
metadata about the file, dimensions, device, and arguments used. This is
useful because \code{tikzDevice::tikz()} itself returns no useful value.
}
\details{
\code{tikzDevice} is a suggested package, not imported. \code{tikz_open()} fails with
a clear error if \code{tikzDevice} is not available. The \code{earnmisc} default for
\code{standAlone} is \code{TRUE}, so generated \code{.tex} files are ready for direct
compilation in the usual case. Metadata are stored internally for
\code{\link[=tikz_info]{tikz_info()}}; \code{tikz_open()} does not assign \code{use.tikz} or other variables
in the caller environment.
}
\examples{
\dontrun{
tikz.info <- tikz_open("figure.tex", width = 14, height = 7)
plot(1:10)
grDevices::dev.off()
tikz_compile(tikz.info)
}

}
